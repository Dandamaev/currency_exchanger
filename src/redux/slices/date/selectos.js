// Список всех дат
export const selectDatesArray = state => state.dates.datesArray;

// Порядок сортировки
export const selectSortOrder = state => state.dates.sortOrder;

// Флаг загрузки
export const selectLoading = state => state.dates.loading;
